name: Deploy Local POC

on:
  workflow_run:
    workflows: ["CI Pipeline"]   # must exactly match ci.yml name
    types:
      - completed

jobs:
  deploy-local:
    runs-on: ubuntu-latest
    env:
      repo: ${{ github.repository }}
      sha: ${{ github.event.workflow_run.head_sha }}
      image: ghcr.io/${{ github.repository }}/cicd-demo:${{ github.event.workflow_run.head_sha }}  # fallback full SHA

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Run Docker container locally
        run: |
          # Use first 7 chars of SHA for image tag
          IMAGE_TAG="${sha:0:7}"
          IMAGE="ghcr.io/${repo}/cicd-demo:${IMAGE_TAG}"

          echo "Running image $image"

          docker rm -f cicd-demo || true
          docker run -d --name cicd-demo -p 8080:80 "$image"
